
// export default function clearCanvas(gameCanvasProps: GameCanvasProps){
//     const{dimensions, ctx} = gameCanvasProps;

//     if (ctx == null) return;

//     const {width, height } = dimensions;

//     ctx.clearRect(0,0, width, height);
// }

// export function drawBackground(gameCanvasProps: GameCanvasProps){
//     const { dimensions, ctx, background } = gameCanvasProps;

//     if (ctx == null) return;

//     const {width, height} = dimensions;

//     ctx.fillStyle = background;
//     ctx.fillRect(0, 0, width, height);
// }