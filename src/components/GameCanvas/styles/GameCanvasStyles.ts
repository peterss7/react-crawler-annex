import { styled } from "@mui/system";

export const Canvas = styled("canvas")(() => ({
  position: "fixed",
  top: 0,
  left: 0,
  width: "100vw",
  height: "100vh",
  display: "block",
  background: "#000",
}));
